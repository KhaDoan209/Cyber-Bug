import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const priorityReducer = createSlice({
  name: 'priorityReducer',
  initialState,
  reducers: {}
});

export const {} = priorityReducer.actions

export default priorityReducer.reducer